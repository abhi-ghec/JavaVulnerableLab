name: CodeQL 
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
 code_analysis:
   uses: advanced-security-demo/central-repo-test/.github/workflows/code_analysis.yml@main
